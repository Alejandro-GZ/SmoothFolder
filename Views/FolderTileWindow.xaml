<Window x:Class="SmoothFolder.Views.FolderTileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="112"
        Height="132"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        AllowDrop="True"
        Topmost="False"
        Opacity="0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="96"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <Border x:Name="FolderCard"
                Width="92"
                Height="92"
                CornerRadius="25"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#7A151B24"
                BorderBrush="#45FFFFFF"
                BorderThickness="1"
                SnapsToDevicePixels="True">
            <Grid Margin="11">
                <UniformGrid x:Name="PreviewGrid"
                             Rows="3"
                             Columns="3"/>
            </Grid>
        </Border>

        <TextBlock x:Name="FolderName"
                   Grid.Row="1"
                   Text="Games"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   FontSize="13"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   TextTrimming="CharacterEllipsis"
                   MaxWidth="108"/>
    </Grid>
</Window>
